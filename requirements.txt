# ATS Resume Scorer — backend dependencies for Vercel deployment.
#
# Note: python-magic-bin (Windows DLL) is replaced with python-magic for Linux (Vercel runs Linux).
# WeasyPrint system libs (Cairo/Pango) are NOT available on Vercel — PDF export will be disabled.

# ── Web framework ──
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
python-multipart>=0.0.9
python-dotenv>=1.0

# ── NLP / ML ──
spacy>=3.7,<4.0
sentence-transformers>=2.5
numpy>=1.26
rapidfuzz>=3.6

# ── File parsing ──
pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1
python-magic>=0.4.27

# ── External services ──
httpx>=0.27
groq>=0.13
PyJWT[crypto]>=2.8

# ── Frontend (Streamlit not needed on Vercel backend) ──
requests>=2.31
supabase>=2.4
